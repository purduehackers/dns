{
  "name": "@purduehackers/dns",
  "private": true,
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "dev": "nitro dev",
    "build": "nitro build",
    "lint": "oxlint --type-aware",
    "format": "oxfmt",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "coverage": "bun test --coverage",
    "cli:validate": "bun run src/index.ts validate",
    "cli:plan": "bun run src/index.ts plan",
    "cli:apply": "bun run src/index.ts apply",
    "cli:pull": "bun run src/index.ts pull"
  },
  "dependencies": {
    "dns": ".",
    "hono": "^4.12.12",
    "nitro": "^3.0.260311-beta",
    "rollup": "^4.60.1",
    "sade": "^1.8.1",
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "@factory/eslint-plugin": "^0.1.0",
    "@types/bun": "latest",
    "@vercel/config": "^0.1.0",
    "eslint": "^10.2.0",
    "oxclippy": "^0.2.0",
    "oxfmt": "^0.44.0",
    "oxlint": "^1.59.0",
    "oxlint-tsgolint": "^0.20.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
